# Python client
```pip install eclipse-zenoh```

## Configure subscriber
Python scripts that listens/publish messages on the same key  the ESP32 is publishing to, i.e., demo/esp32.

## Create a Python Subscriber Script
Connect to the publicly availble zenoh broker: "demo.zenoh.io". Test connex=ction with `telnet demo.zenoh.io`



